/* eslint-disable no-console */
const {DEBUG} = process.env

module.exports = function cds_debug (x='all', prefix='[cds] -') {
  const re = RegExp(`\\b(y|all|${x})\\b`)
  if (re.test(DEBUG)) return ((...args) => console.warn (prefix, ...args))
}
